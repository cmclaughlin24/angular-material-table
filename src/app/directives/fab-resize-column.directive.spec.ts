import { FabResizeColumnDirective } from './fab-resize-column.directive';

describe('FabResizeColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new FabResizeColumnDirective();
    expect(directive).toBeTruthy();
  });
});
