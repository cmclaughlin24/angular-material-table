import { MatColumnResizeDirective } from './mat-column-resize.directive';

describe('MatColumnResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new MatColumnResizeDirective();
    expect(directive).toBeTruthy();
  });
});
